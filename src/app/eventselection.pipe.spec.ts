import { EventselectionPipe } from './eventselection.pipe';

describe('EventselectionPipe', () => {
  it('create an instance', () => {
    const pipe = new EventselectionPipe();
    expect(pipe).toBeTruthy();
  });
});
